import numpy as np

import numpy as np

x = 1.0		#define a float
y = 2.0		#define another float

# Exponents and logarithms
print(np.exp(x))
print(np.log(x))
print(np.log10(x))
print(np.log2(x))

# Min/max/misc
print(np.fabs(x))
print(np.fmin(x,y))
print(np.fmax(x,y))

# Populate arrays